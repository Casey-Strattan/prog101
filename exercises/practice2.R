# Calling a function is name followed by parentheses, arguments inside.
sqrt(9)
abs(-3)

# Autocomplete function in the editor is a useful IDE feature

# str() is structure
str(3)
str("Santa Barbara")

# Syntax highlighting

# Functions can take multiple arguments
pi
round(pi,0)
round(pi, 5)

n_digits <- 2
round(pi, n_digits)

# Functions are a great example of encapsulation. Replacing potentially complex
# logic with simple names makes our code more readable.

?sqrt
